Handles the data from dToT and the calibrated data (peak position at all channels) 

First do synchronization of the files in .csv format to merge two DAQ systems  

Use Java to do the spline (for speed) and python to draw
